import { LightningElement,api } from 'lwc';

export default class Practice_Slot extends LightningElement {
 // @api meetingRoomInfo = {roomName:'A-01', roomCapacity:'12'};
  @api showRoomInfo = false;
}